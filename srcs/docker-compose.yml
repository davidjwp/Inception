version: "3"
  services:
    nginx:
      build: 
        context: ./requirements/nginx
        dockerfile: Dockerfile
      image: nginx:latest
      ports:
        - "430:430"

    mariadb:
      
    wordpress:
      volumes:
        - DB: requirements/wordpress
